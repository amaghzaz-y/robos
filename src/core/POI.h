#pragma once
#include "utils/utils.h"

namespace POI
{
	// ------------------------------------------
	// Departure Areas - Center
	const Vec2 yStart = Vec2(1225, 1775); // Yellow
	const Vec2 yEnd = Vec2(375, 225);	  // Yellow
}